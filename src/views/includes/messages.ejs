<% if(errors.length > 0) { %>
<div class="row">
  <div class="col">
    <div class="alert alert-danger">
      <% errors.forEach(erro => { %> <%= erro %><br />

      <% }); %>
    </div>
  </div>
</div>

<% } %> <% if(sucess.length > 0) { %>
<div class="row">
  <div class="col">
    <div class="alert bg-success">
      <% sucess.forEach(erro => { %> <%= erro %><br />

      <% }); %>
    </div>
  </div>
</div>

<% } %>
